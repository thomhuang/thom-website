{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Edge",
      "port": 3000,
      "request": "attach",
      "type": "msedge",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
