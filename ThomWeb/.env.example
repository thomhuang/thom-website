# The API base URL. Unset by default: development uses http://localhost:4000
# and production uses /api (same-origin, proxied by the Worker to thom-server).
# Set this only to point a build at a different API. VITE_* values are
# embedded in the client bundle, so never put secrets here.
# VITE_API_URL=
