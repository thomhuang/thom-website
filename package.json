{
  "name": "thom-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "cd ThomWeb && npm ci && npm run build",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --test worker/index.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.132.0"
  }
}
